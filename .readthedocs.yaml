version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.8"
  # 多语言配置
  locale:
    - en
    - zh_CN

sphinx:
  configuration: docs/source/conf.py
  fail_on_warning: false

formats:
  - pdf
  - epub

python:
  install:
    - requirements: requirements.txt

